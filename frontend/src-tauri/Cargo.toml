[package]
name = "litradar"
version = "0.1.0"
description = "Personal research radar desktop app"
authors = ["LitRadar"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
default = []
